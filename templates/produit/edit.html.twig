{% include 'base.html.twig' %}

{% block body %}
    <h1>Edition de Produit</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'modifier'}) }}

    <a href="{{ path('produit_index') }}">retourner a la liste des produits</a>

    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}