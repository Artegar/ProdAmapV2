{% include 'base.html.twig' %}

{% block body %}
    <h1>Producteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producteur.prodId }}</td>
            </tr>

            {#<tr>#}
                {#<th>Verifier</th>#}
                {#<td>{{ producteur.prodVerif ? 'Oui' : 'Non' }}</td>#}
            {#</tr>#}

            <tr>
                <th>Numéro SIREN</th>
                <td>{{ producteur.prodSiren }}</td>
            </tr>
            <tr>
                <th>Date de vérification</th>
                <td>{{ producteur.prodDateVerif ? producteur.prodDateVerif|date('H:i:s d-m-Y') : '' }}</td>
            </tr>
            <tr>
                <th>Nom de l'exploitation</th>
                <td>{{ producteur.prodNomExploit }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('producteur_index') }}">retourner à la liste</a>

    </br>

    <a href="{{ path('producteur_edit', {'prodId': producteur.prodId}) }}">modifier</a>

    {{ include('producteur/_delete_form.html.twig') }}
{% endblock %}