<form method="post" action="{{ path('adherant_delete', {'adherId': adherant.adherId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adherant.adherId) }}">
    <button class="btn">Delete</button>
</form>