{% include 'base.html.twig' %}

{% block body %}

<h1 style="margin-top: 50px;">Famille index</h1>
<div class="container-fluid" style="margin-top: 50px;">
    <div class="row">
        <div class="col-md-12">
            <table class="table">
                <thead>
                    <tr>
                        <th>Famille numéro</th>
                        <th>Nom</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for famille in familles %}
                    <tr class="table-active">
                        <td>{{ famille.famId }}</td>
                        <td>{{ famille.famNom }}</td>
                        <td>
                            <a href="{{ path('famille_show', {'famId': famille.famId}) }}">show</a>
                            <a href="{{ path('famille_edit', {'famId': famille.famId}) }}">edit</a>
                        </td>
                    </tr>
                    {% else %}
                    <tr>
                        <td colspan="3">no records found</td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

<a href="{{ path('famille_new') }}">Create new</a>
{% endblock %}