{% include 'base.html.twig' %}


{% block body %}
    <h1>Famille</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ famille.famNom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('famille_index') }}">retourner a la liste de famille</a>

    </br> 
    <a href="{{ path('famille_edit', {'famId': famille.famId}) }}">modifier</a>

    {{ include('famille/_delete_form.html.twig') }}
{% endblock %}