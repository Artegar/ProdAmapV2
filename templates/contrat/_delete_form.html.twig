<form method="post" action="{{ path('contrat_delete', {'contId': contrat.contId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contrat.contId) }}">
    <button class="btn">Delete</button>
</form>